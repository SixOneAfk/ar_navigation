{
  "name": "ar-nav-monorepo",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "start:dev:all": "(npm --prefix apps/core-backend run start:dev & npm --prefix apps/gateway run start:dev & npm --prefix apps/client run dev -- --host & wait)",
    "start:dev:all:with-cv": "bash start-dev-all.sh",
    "setup:cv-service": "cd apps/cv-service && python3 -m venv .venv && source .venv/bin/activate && pip install --extra-index-url https://download.pytorch.org/whl/cpu -r requirements-cpu.txt && pip install -r requirements.txt && cd ../..",
    "start:cv": "cd apps/cv-service && source .venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
  }
}
